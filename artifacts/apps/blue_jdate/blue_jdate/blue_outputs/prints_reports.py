from __future__ import annotations

import frappe
from frappe import _

# BLUE Outputs – Report Print builders (auto-extracted from utils.py)
# Each function creates/updates a custom Report (print-friendly / Jalali-aware).

def create_blue_report_ap_summary_v1():
    """Create/Update BLUE_REPORT_AP_SUMMARY_V1 Print Format (Jinja) for Accounts Payable Summary."""
    import frappe

    NAME = "BLUE_REPORT_AP_SUMMARY_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .baps-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .baps-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .baps-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .baps-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .baps-meta div {
    margin-bottom: 2px;
  }

  .baps-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .baps-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .baps-table th,
  .baps-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .baps-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .baps-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .baps-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="baps-root">

  <div class="baps-title">گزارش خلاصه حساب‌های پرداختنی</div>
  <div class="baps-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      Accounts Payable Summary
    {% endif %}
  </div>

  <div class="baps-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="baps-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% if filters.get("supplier_group") %}
      <div>
        <span class="baps-meta-label">گروه تأمین‌کننده:</span>
        {{ filters.get("supplier_group") }}
      </div>
      {% endif %}

      {% if filters.get("ageing_based_on") %}
      <div>
        <span class="baps-meta-label">مبنای سررسید:</span>
        {{ filters.get("ageing_based_on") }}
      </div>
      {% endif %}

      {% set report_date = filters.get("report_date") or filters.get("to_date") %}
      {% set from_date = filters.get("from_date") %}
      {% if from_date %}
      <div>
        <span class="baps-meta-label">از تاریخ:</span>
        {{ from_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
      {% if report_date %}
      <div>
        <span class="baps-meta-label">تا تاریخ/تاریخ گزارش:</span>
        {{ report_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {# سلول فرمت‌شده برای سطر/ستون خلاصه AP #}
  {% macro blue_ap_summary_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime")
          or col.fieldname in ("posting_date", "bill_date", "due_date", "report_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int")
          or col.fieldname in ("outstanding_amount", "invoiced", "paid", "balance") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="baps-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_ap_summary_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="baps-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_ap_v1():
    """Create/Update BLUE_REPORT_AP_V1 Print Format (Jinja) for Accounts Payable."""
    import frappe

    NAME = "BLUE_REPORT_AP_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bap-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bap-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bap-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bap-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bap-meta div {
    margin-bottom: 2px;
  }

  .bap-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bap-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bap-table th,
  .bap-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bap-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bap-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bap-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bap-root">

  <div class="bap-title">گزارش حساب‌های پرداختنی</div>
  <div class="bap-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      Accounts Payable
    {% endif %}
  </div>

  <div class="bap-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bap-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% if filters.get("supplier") %}
      <div>
        <span class="bap-meta-label">تأمین‌کننده:</span>
        {{ filters.get("supplier") }}
      </div>
      {% endif %}

      {% if filters.get("supplier_group") %}
      <div>
        <span class="bap-meta-label">گروه تأمین‌کننده:</span>
        {{ filters.get("supplier_group") }}
      </div>
      {% endif %}

      {% set report_date = filters.get("report_date") or filters.get("to_date") %}
      {% set from_date = filters.get("from_date") %}
      {% if from_date %}
      <div>
        <span class="bap-meta-label">از تاریخ:</span>
        {{ from_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
      {% if report_date %}
      <div>
        <span class="bap-meta-label">تا تاریخ/تاریخ گزارش:</span>
        {{ report_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {# سلول فرمت‌شده برای سطر/ستون حساب‌های پرداختنی #}
  {% macro blue_ap_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime")
          or col.fieldname in ("posting_date", "bill_date", "due_date", "report_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bap-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_ap_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bap-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_ar_ageing_v1():
    """Create/Update BLUE_REPORT_AR_AGEING_V1 Print Format (Jinja) for Accounts Receivable Ageing."""
    import frappe

    NAME = "BLUE_REPORT_AR_AGEING_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bara-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bara-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bara-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bara-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bara-meta div {
    margin-bottom: 2px;
  }

  .bara-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bara-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bara-table th,
  .bara-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bara-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bara-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bara-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bara-root">

  <div class="bara-title">گزارش مانده سنی حساب‌های دریافتنی</div>
  <div class="bara-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      Accounts Receivable Ageing
    {% endif %}
  </div>

  <div class="bara-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bara-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% if filters.get("customer_group") %}
      <div>
        <span class="bara-meta-label">گروه مشتری:</span>
        {{ filters.get("customer_group") }}
      </div>
      {% endif %}

      {% if filters.get("ageing_based_on") %}
      <div>
        <span class="bara-meta-label">مبنای سررسید:</span>
        {{ filters.get("ageing_based_on") }}
      </div>
      {% endif %}

      {% set report_date = filters.get("report_date") or filters.get("to_date") %}
      {% set from_date = filters.get("from_date") %}
      {% if from_date %}
      <div>
        <span class="bara-meta-label">از تاریخ:</span>
        {{ from_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
      {% if report_date %}
      <div>
        <span class="bara-meta-label">تا تاریخ/تاریخ گزارش:</span>
        {{ report_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {# سلول فرمت‌شده برای ردیف/ستون Ageing #}
  {% macro blue_ar_ageing_cell(row, col) -%}
    {% set fname = col.fieldname if col.fieldname is defined else None %}
    {% set val = row.get(fname) if fname else None %}

    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime")
          or fname in ("posting_date", "invoice_date", "due_date", "report_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int")
          or "amount" in (fname or "") 
          or "balance" in (fname or "") 
          or "outstanding" in (fname or "") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bara-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_ar_ageing_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bara-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_ar_summary_v1():
    """Create/Update BLUE_REPORT_AR_SUMMARY_V1 Print Format (Jinja) for Accounts Receivable Summary."""
    import frappe

    NAME = "BLUE_REPORT_AR_SUMMARY_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bars-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bars-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bars-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bars-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bars-meta div {
    margin-bottom: 2px;
  }

  .bars-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bars-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bars-table th,
  .bars-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bars-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bars-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bars-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bars-root">

  <div class="bars-title">گزارش خلاصه حساب‌های دریافتنی</div>
  <div class="bars-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      Accounts Receivable Summary
    {% endif %}
  </div>

  <div class="bars-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bars-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% if filters.get("customer_group") %}
      <div>
        <span class="bars-meta-label">گروه مشتری:</span>
        {{ filters.get("customer_group") }}
      </div>
      {% endif %}

      {% if filters.get("territory") %}
      <div>
        <span class="bars-meta-label">منطقه:</span>
        {{ filters.get("territory") }}
      </div>
      {% endif %}

      {% set report_date = filters.get("report_date") or filters.get("to_date") %}
      {% set from_date = filters.get("from_date") %}
      {% if from_date %}
      <div>
        <span class="bars-meta-label">از تاریخ:</span>
        {{ from_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
      {% if report_date %}
      <div>
        <span class="bars-meta-label">تا تاریخ/تاریخ گزارش:</span>
        {{ report_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {# سلول فرمت‌شده برای سطر/ستون گزارش خلاصه دریافتنی #}
  {% macro blue_ar_summary_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime")
          or col.fieldname in ("posting_date", "invoice_date", "due_date", "report_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bars-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_ar_summary_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bars-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_ar_v1():
    """Create/Update BLUE_REPORT_AR_V1 Print Format (Jinja) for Accounts Receivable."""
    import frappe

    NAME = "BLUE_REPORT_AR_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bar-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bar-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bar-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bar-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bar-meta div {
    margin-bottom: 2px;
  }

  .bar-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bar-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bar-table th,
  .bar-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bar-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bar-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bar-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bar-root">

  <div class="bar-title">گزارش حساب‌های دریافتنی</div>
  <div class="bar-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      Accounts Receivable
    {% endif %}
  </div>

  <div class="bar-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bar-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% if filters.get("customer") %}
      <div>
        <span class="bar-meta-label">مشتری:</span>
        {{ filters.get("customer") }}
      </div>
      {% endif %}

      {% if filters.get("customer_group") %}
      <div>
        <span class="bar-meta-label">گروه مشتری:</span>
        {{ filters.get("customer_group") }}
      </div>
      {% endif %}

      {% if filters.get("territory") %}
      <div>
        <span class="bar-meta-label">منطقه:</span>
        {{ filters.get("territory") }}
      </div>
      {% endif %}

      {% set report_date = filters.get("report_date") or filters.get("to_date") %}
      {% set from_date = filters.get("from_date") %}
      {% if from_date %}
      <div>
        <span class="bar-meta-label">از تاریخ:</span>
        {{ from_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
      {% if report_date %}
      <div>
        <span class="bar-meta-label">تا تاریخ/تاریخ گزارش:</span>
        {{ report_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {# سلول فرمت‌شده برای سطر/ستون گزارش دریافتنی #}
  {% macro blue_ar_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime")
          or col.fieldname in ("posting_date", "invoice_date", "due_date", "report_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bar-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_ar_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bar-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_asset_depr_schedule_v1():
    """Create/Update BLUE_REPORT_ASSET_DEPR_SCHEDULE_V1 for Asset Depreciation Schedule."""
    import frappe

    NAME = "BLUE_REPORT_ASSET_DEPR_SCHEDULE_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Asset Depreciation Schedule" }}
  </div>
  <div class="blue-report-subtitle">
    برنامه استهلاک دارایی‌های ثابت (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("company") %}
          شرکت: {{ filters.get("company") }}
        {% endif %}
      </span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه گزارش:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("asset_category") %}
          گروه دارایی: {{ filters.get("asset_category") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("finance_book") %}
          دفتر مالی: {{ filters.get("finance_book") }}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("status") %}
          وضعیت دارایی: {{ filters.get("status") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("frequency") %}
          تناوب استهلاک: {{ filters.get("frequency") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') or row.get('is_total_row') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش Asset Depreciation Schedule ساخته شده و ساختار ستون‌ها، دوره‌ها و جمع‌ها
    مطابق تنظیمات گزارش اصلی است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_asset_register_v1():
    """Create/Update BLUE_REPORT_ASSET_REGISTER_V1 for Fixed Asset Register."""
    import frappe

    NAME = "BLUE_REPORT_ASSET_REGISTER_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Fixed Asset Register" }}
  </div>
  <div class="blue-report-subtitle">
    ثبت و وضعیت دارایی‌های ثابت (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("company") %}
          شرکت: {{ filters.get("company") }}
        {% endif %}
      </span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه گزارش:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("asset_category") %}
          گروه دارایی: {{ filters.get("asset_category") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("asset_location") %}
          محل استقرار: {{ filters.get("asset_location") }}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("finance_book") %}
          دفتر مالی: {{ filters.get("finance_book") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("status") %}
          وضعیت دارایی: {{ filters.get("status") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') or row.get('is_total_row') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش Fixed Asset Register ساخته شده و ساختار ستون‌ها، گروه‌بندی و جمع‌ها
    مطابق تنظیمات گزارش اصلی است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_attendance_sheet_v1():
    """Create/Update BLUE_REPORT_ATTENDANCE_SHEET_V1 for Attendance Sheet / Summary report."""
    import frappe

    NAME = "BLUE_REPORT_ATTENDANCE_SHEET_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Attendance Sheet" }}
  </div>
  <div class="blue-report-subtitle">
    گزارش حضور و غیاب کارکنان (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>شرکت: {{ filters.get("company") if filters else "" }}</span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه گزارش:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("employee") %}
          کد کارمند: {{ filters.get("employee") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("employee_name") %}
          نام کارمند: {{ filters.get("employee_name") }}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("department") %}
          دپارتمان: {{ filters.get("department") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("branch") %}
          شعبه: {{ filters.get("branch") }}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("attendance_status") %}
          وضعیت حضور: {{ filters.get("attendance_status") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("shift") %}
          شیفت: {{ filters.get("shift") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') or row.get('is_total_row') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش «Attendance» / «Attendance Sheet» در ERPNext تولید شده است.
    ردیف‌ها و جمع‌های کلی مطابق فیلترها و تنظیمات گزارش اصلی نمایش داده می‌شوند.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_balance_sheet_v1():
    """Create/Update BLUE_REPORT_BALANCE_SHEET_V1 Print Format (Jinja) for Balance Sheet report."""
    import frappe

    NAME = "BLUE_REPORT_BALANCE_SHEET_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bbs-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bbs-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bbs-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bbs-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bbs-meta div {
    margin-bottom: 2px;
  }

  .bbs-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bbs-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bbs-table th,
  .bbs-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bbs-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bbs-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bbs-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bbs-root">

  <div class="bbs-title">ترازنامه</div>
  <div class="bbs-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      ترازنامه (Balance Sheet)
    {% endif %}
  </div>

  <div class="bbs-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bbs-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% set to_date = filters.get("to_date") %}
      {% if to_date %}
      <div>
        <span class="bbs-meta-label">تا تاریخ:</span>
        {{ to_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {% macro blue_bs_format_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime") or col.fieldname in ("to_date", "from_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bbs-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_bs_format_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bbs-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_bank_reco_v1():
    """Create/Update BLUE_REPORT_BANK_RECO_V1 Print Format (Jinja) for Bank Reconciliation Statement."""
    import frappe

    NAME = "BLUE_REPORT_BANK_RECO_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bbr-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bbr-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bbr-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bbr-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bbr-meta div {
    margin-bottom: 2px;
  }

  .bbr-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bbr-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bbr-table th,
  .bbr-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bbr-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bbr-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bbr-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bbr-root">

  <div class="bbr-title">گزارش مغایرت بانکی</div>
  <div class="bbr-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      Bank Reconciliation Statement
    {% endif %}
  </div>

  <div class="bbr-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bbr-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% if filters.get("bank_account") %}
      <div>
        <span class="bbr-meta-label">حساب بانکی:</span>
        {{ filters.get("bank_account") }}
      </div>
      {% endif %}

      {% set from_date = filters.get("from_date") %}
      {% set to_date = filters.get("to_date") %}

      {% if from_date %}
      <div>
        <span class="bbr-meta-label">از تاریخ:</span>
        {{ from_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}

      {% if to_date %}
      <div>
        <span class="bbr-meta-label">تا تاریخ:</span>
        {{ to_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {# سلول فرمت‌شده (تاریخ جلالی، اعداد با کاما) #}
  {% macro blue_bank_reco_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime") or col.fieldname in ("posting_date", "date", "from_date", "to_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bbr-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_bank_reco_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bbr-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_bom_explosion_v1():
    """Create/Update BLUE_REPORT_BOM_EXPLOSION_V1 as a Report Builder on BOM."""
    import frappe

    NAME = "BLUE_REPORT_BOM_EXPLOSION_V1"

    HTML = """
<style>
  .bapi-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bapi-report-title {
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 6px;
    text-align: center;
  }

  .bapi-report-subtitle {
    font-size: 10pt;
    margin-bottom: 10px;
    text-align: center;
    color: #666;
  }

  .bapi-report-filters {
    font-size: 9pt;
    margin-bottom: 10px;
    border: 1px solid #eee;
    padding: 6px 8px;
    border-radius: 3px;
  }

  .bapi-report-filters-title {
    font-weight: 600;
    margin-bottom: 4px;
  }

  .bapi-report-filters-table {
    width: 100%;
    border-collapse: collapse;
  }

  .bapi-report-filters-table td {
    padding: 2px 4px;
    vertical-align: top;
  }

  .bapi-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
    margin-top: 6px;
  }

  .bapi-report-table th,
  .bapi-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .bapi-report-table th {
    background-color: #fafafa;
    font-weight: 600;
    text-align: center;
    white-space: nowrap;
  }

  .bapi-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bapi-report-footer {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #777;
    text-align: left;
  }
</style>

<div class="bapi-report-root">

  <div class="bapi-report-title">
    گزارش انفجار BOM / تحلیل اقلام
  </div>
  <div class="bapi-report-subtitle">
    {{ report_name }}
  </div>

  {% if filters %}
  <div class="bapi-report-filters">
    <div class="bapi-report-filters-title">فیلترها:</div>
    <table class="bapi-report-filters-table">
      {% for key, value in filters.items() %}
      {% if value %}
      <tr>
        <td><strong>{{ key }}</strong></td>
        <td>{{ value }}</td>
      </tr>
      {% endif %}
      {% endfor %}
    </table>
  </div>
  {% endif %}

  <table class="bapi-report-table">
    <thead>
      <tr>
        {% for col in columns %}
        <th>
          {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
        </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
        {% set fname = col.fieldname %}
        <td>
          {% if fname %}
            {{ row.get(fname) }}
          {% else %}
            {{ row.get(col.label) if row.get(col.label) is defined else "" }}
          {% endif %}
        </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bapi-report-footer">
    چاپ شده توسط BlueAPI / BlueJDate
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Report", NAME)

    if existing:
        report = frappe.get_doc("Report", existing)
    else:
        # ریپورت جدید از نوع Report Builder روی BOM
        report = frappe.get_doc({
            "doctype": "Report",
            "report_name": NAME,
            "ref_doctype": "BOM",
            "report_type": "Report Builder",
            "is_standard": "No",
            "disabled": 0,
            "module": "Manufacturing",
        })
        report.insert(ignore_permissions=True)

    # آپدیت نهایی تنظیمات چاپ
    report.is_standard = "No"
    report.disabled = 0
    report.custom_format = 1
    if hasattr(report, "letter_head"):
        report.letter_head = None
    report.html = HTML
    report.save()

    frappe.db.commit()
    print("REPORT:", report.name, "created/updated.")

def create_blue_report_cash_flow_v1():
    """Create/Update BLUE_REPORT_CASH_FLOW_V1 Print Format (Jinja) for Cash Flow Statement."""
    import frappe

    NAME = "BLUE_REPORT_CASH_FLOW_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bcf-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bcf-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bcf-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bcf-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bcf-meta div {
    margin-bottom: 2px;
  }

  .bcf-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bcf-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bcf-table th,
  .bcf-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bcf-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bcf-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bcf-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bcf-root">

  <div class="bcf-title">صورت جریان وجوه نقد</div>
  <div class="bcf-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      Cash Flow Statement
    {% endif %}
  </div>

  <div class="bcf-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bcf-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% set from_date = filters.get("from_date") %}
      {% set to_date = filters.get("to_date") %}

      {% if from_date %}
      <div>
        <span class="bcf-meta-label">از تاریخ:</span>
        {{ from_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}

      {% if to_date %}
      <div>
        <span class="bcf-meta-label">تا تاریخ:</span>
        {{ to_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {% macro blue_cflow_format_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime") or col.fieldname in ("posting_date", "from_date", "to_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bcf-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_cflow_format_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bcf-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_gl_v1():
    """Create/Update BLUE_REPORT_GL_V1 Print Format (Jinja) for General Ledger report."""
    import frappe

    NAME = "BLUE_REPORT_GL_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bgl-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bgl-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bgl-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bgl-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bgl-meta div {
    margin-bottom: 2px;
  }

  .bgl-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bgl-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bgl-table th,
  .bgl-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bgl-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bgl-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bgl-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bgl-root">

  <div class="bgl-title">دفتر کل</div>
  <div class="bgl-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      دفتر کل (General Ledger)
    {% endif %}
  </div>

  <div class="bgl-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bgl-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% set from_date = filters.get("from_date") %}
      {% set to_date = filters.get("to_date") %}
      {% if from_date or to_date %}
      <div>
        <span class="bgl-meta-label">دوره گزارش:</span>
        {% if from_date %} از {{ from_date | as_jdate("YYYY/MM/DD") }} {% endif %}
        {% if to_date %} تا {{ to_date | as_jdate("YYYY/MM/DD") }} {% endif %}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {% macro blue_gl_format_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime") or col.fieldname in ("posting_date", "from_date", "to_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bgl-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_gl_format_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bgl-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_leave_balance_v1():
    """Create/Update BLUE_REPORT_LEAVE_BALANCE_V1 for Employee Leave Balance report."""
    import frappe

    NAME = "BLUE_REPORT_LEAVE_BALANCE_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Employee Leave Balance" }}
  </div>
  <div class="blue-report-subtitle">
    گزارش مانده مرخصی کارکنان (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>شرکت: {{ filters.get("company") if filters else "" }}</span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date") or filters.get("as_of_date")) %}
          بازه / تاریخ مرجع:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("as_of_date") %}
            تا تاریخ {{ filters.get("as_of_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("employee") %}
          کارمند: {{ filters.get("employee") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("employee_name") %}
          نام کارمند: {{ filters.get("employee_name") }}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("department") %}
          دپارتمان: {{ filters.get("department") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("branch") %}
          شعبه: {{ filters.get("branch") }}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("leave_type") %}
          نوع مرخصی: {{ filters.get("leave_type") }}
        {% endif %}
      </span>
      <span></span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش «Employee Leave Balance» در ERPNext تولید شده است.
    مقادیر مانده، بر اساس فیلترهای انتخاب‌شده (بازه زمانی / تاریخ مرجع) محاسبه شده‌اند.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_leave_ledger_v1():
    """Create/Update BLUE_REPORT_LEAVE_LEDGER_V1 for Leave Ledger report."""
    import frappe

    NAME = "BLUE_REPORT_LEAVE_LEDGER_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Leave Ledger" }}
  </div>
  <div class="blue-report-subtitle">
    دفتر ثبت مرخصی کارکنان (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>شرکت: {{ filters.get("company") if filters else "" }}</span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("employee") %}
          کارمند: {{ filters.get("employee") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("employee_name") %}
          نام کارمند: {{ filters.get("employee_name") }}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("department") %}
          دپارتمان: {{ filters.get("department") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("branch") %}
          شعبه: {{ filters.get("branch") }}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("leave_type") %}
          نوع مرخصی: {{ filters.get("leave_type") }}
        {% endif %}
      </span>
      <span></span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش «Leave Ledger» در ERPNext تولید شده است.
    چیدمان و جمع‌ها مطابق خروجی خود گزارش است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_payroll_analytics_v1():
    """Create/Update BLUE_REPORT_PAYROLL_ANALYTICS_V1 for Payroll Analytics report."""
    import frappe

    NAME = "BLUE_REPORT_PAYROLL_ANALYTICS_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Payroll Analytics" }}
  </div>
  <div class="blue-report-subtitle">
    گزارش تحلیلی حقوق و دستمزد (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>شرکت: {{ filters.get("company") if filters else "" }}</span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("department") %}
          دپارتمان: {{ filters.get("department") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("branch") %}
          شعبه: {{ filters.get("branch") }}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("designation") %}
          سمت: {{ filters.get("designation") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("payroll_entry") %}
          دوره پرداخت: {{ filters.get("payroll_entry") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش «Payroll Analytics» در ERPNext تولید شده است.
    چیدمان، گروه‌بندی و جمع‌ها مطابق خروجی خود گزارش است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_pl_v1():
    """Create/Update BLUE_REPORT_PL_V1 Print Format (Jinja) for Profit and Loss report."""
    import frappe

    NAME = "BLUE_REPORT_PL_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .bpl-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bpl-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .bpl-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .bpl-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .bpl-meta div {
    margin-bottom: 2px;
  }

  .bpl-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .bpl-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .bpl-table th,
  .bpl-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .bpl-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .bpl-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bpl-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="bpl-root">

  <div class="bpl-title">صورت سود و زیان</div>
  <div class="bpl-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      Profit and Loss Statement
    {% endif %}
  </div>

  <div class="bpl-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="bpl-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% set from_date = filters.get("from_date") %}
      {% set to_date = filters.get("to_date") %}

      {% if from_date %}
      <div>
        <span class="bpl-meta-label">از تاریخ:</span>
        {{ from_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}

      {% if to_date %}
      <div>
        <span class="bpl-meta-label">تا تاریخ:</span>
        {{ to_date | as_jdate("YYYY/MM/DD") }}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {% macro blue_pl_format_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime") or col.fieldname in ("posting_date", "from_date", "to_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="bpl-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_pl_format_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bpl-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_production_planning_v1():
    """Create/Update BLUE_REPORT_PRODUCTION_PLANNING_V1 for Production Planning report."""
    import frappe

    NAME = "BLUE_REPORT_PRODUCTION_PLANNING_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Production Planning" }}
  </div>
  <div class="blue-report-subtitle">
    برنامه‌ریزی تولید (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("company") %}
          شرکت: {{ filters.get("company") }}
        {% endif %}
      </span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه برنامه‌ریزی:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("status") %}
          وضعیت سفارش‌ها / برنامه‌ها: {{ filters.get("status") }}
        {% endif %}
      </span>
      <span>
        {% if filters and (filters.get("production_item") or filters.get("item_code")) %}
          قلم تولیدی:
          {{ filters.get("production_item") or filters.get("item_code") }}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("sales_order") %}
          بر اساس سفارش فروش: {{ filters.get("sales_order") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("for_warehouse") %}
          انبار هدف: {{ filters.get("for_warehouse") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') or row.get('is_total_row') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس ساختار گزارش «Production Planning» ساخته شده و ستون‌ها، گروه‌بندی‌ها و جمع‌ها
    مطابق تنظیمات گزارش اصلی است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_project_billing_v1():
    """Create/Update BLUE_REPORT_PROJECT_BILLING_V1 for Project Billing / Profitability."""
    import frappe

    NAME = "BLUE_REPORT_PROJECT_BILLING_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Project Billing Summary" }}
  </div>
  <div class="blue-report-subtitle">
    صورت وضعیت مالی پروژه (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("company") %}
          شرکت: {{ filters.get("company") }}
        {% endif %}
      </span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه گزارش:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("project") %}
          پروژه: {{ filters.get("project") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("employee") %}
          مدیر / مسئول پروژه: {{ filters.get("employee") }}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("customer") %}
          مشتری: {{ filters.get("customer") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("group_by") %}
          گروه‌بندی بر اساس: {{ filters.get("group_by") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') or row.get('is_total_row') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش‌های Project Billing / Project Profitability تولید شده است.
    ساختار سطرها، جمع‌ها و گروه‌بندی دقیقاً مطابق تنظیمات گزارش اصلی است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_project_timesheet_v1():
    """Create/Update BLUE_REPORT_PROJECT_TIMESHEET_V1 for Project-wise Timesheet Summary."""
    import frappe

    NAME = "BLUE_REPORT_PROJECT_TIMESHEET_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Project Timesheet Summary" }}
  </div>
  <div class="blue-report-subtitle">
    خلاصه زمان‌کار پروژه (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("company") %}
          شرکت: {{ filters.get("company") }}
        {% endif %}
      </span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه گزارش:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("project") %}
          پروژه: {{ filters.get("project") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("employee") %}
          کارمند: {{ filters.get("employee") }}
        {% endif %}
      </span>
    </div>

    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("customer") %}
          مشتری: {{ filters.get("customer") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("group_by") %}
          گروه‌بندی بر اساس: {{ filters.get("group_by") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') or row.get('is_total_row') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش‌های زمان‌کار (Timesheet / Project-wise Timesheet) تولید شده است.
    ساختار سطرها، جمع‌ها و گروه‌بندی دقیقاً مطابق تنظیمات گزارش اصلی است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_purchase_analytics_v1():
    """Create/Update BLUE_REPORT_PURCHASE_ANALYTICS_V1 for Purchase Analytics report."""
    NAME = "BLUE_REPORT_PURCHASE_ANALYTICS_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Purchase Analytics" }}
  </div>
  <div class="blue-report-subtitle">
    گزارش تحلیلی خرید (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>شرکت: {{ filters.get("company") if filters else "" }}</span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("supplier") %}
          تأمین‌کننده: {{ filters.get("supplier") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("supplier_group") %}
          گروه تأمین‌کننده: {{ filters.get("supplier_group") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش «گزارش تحلیلی خرید» در ERPNext تولید شده است. چیدمان و گروه‌بندی بر اساس تنظیمات خود گزارش است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_salary_register_v1():
    """Create/Update BLUE_REPORT_SALARY_REGISTER_V1 for Salary Register report."""
    import frappe

    NAME = "BLUE_REPORT_SALARY_REGISTER_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Salary Register" }}
  </div>
  <div class="blue-report-subtitle">
    گزارش ریز حقوق و دستمزد (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>شرکت: {{ filters.get("company") if filters else "" }}</span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه:
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("payroll_entry") %}
          دوره پرداخت: {{ filters.get("payroll_entry") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("employee") %}
          کارمند: {{ filters.get("employee") }}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("department") %}
          دپارتمان: {{ filters.get("department") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("branch") %}
          شعبه: {{ filters.get("branch") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش «Salary Register» در ERPNext تولید شده است.
    چیدمان و گروه‌بندی بر اساس تنظیمات خود گزارش است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_sales_analytics_v1():
    """Create/Update BLUE_REPORT_SALES_ANALYTICS_V1 for Sales Analytics report."""
    NAME = "BLUE_REPORT_SALES_ANALYTICS_V1"
    DOCTYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .blue-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 11pt;
    color: #222;
    padding: 24px 32px;
  }

  .blue-report-title {
    text-align: center;
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .blue-report-subtitle {
    text-align: center;
    font-size: 10pt;
    color: #555;
    margin-bottom: 16px;
  }

  .blue-report-meta {
    margin-bottom: 12px;
    font-size: 9pt;
  }

  .blue-report-meta-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2px;
  }

  .blue-report-meta-row span {
    display: inline-block;
  }

  .blue-report-orange-line {
    margin: 8px 0 12px 0;
    border-top: 2px solid #ff8c00;
  }

  .blue-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
  }

  .blue-report-table th,
  .blue-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .blue-report-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .blue-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .blue-report-row-group {
    background-color: #fff7e6;
    font-weight: 600;
  }

  .blue-report-footer-note {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #666;
  }
</style>

<div class="blue-report-root">

  <div class="blue-report-title">
    {{ report_name or "Sales Analytics" }}
  </div>
  <div class="blue-report-subtitle">
    گزارش تحلیلی فروش (BlueAPI)
  </div>

  <div class="blue-report-meta">
    <div class="blue-report-meta-row">
      <span>شرکت: {{ filters.get("company") if filters else "" }}</span>
      <span>
        {% if filters and (filters.get("from_date") or filters.get("to_date")) %}
          بازه: 
          {% if filters.get("from_date") %}
            از {{ filters.get("from_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
          {% if filters.get("to_date") %}
            تا {{ filters.get("to_date") | as_jdate("YYYY/MM/DD") }}
          {% endif %}
        {% endif %}
      </span>
    </div>
    <div class="blue-report-meta-row">
      <span>
        {% if filters and filters.get("sales_person") %}
          کارشناس فروش: {{ filters.get("sales_person") }}
        {% endif %}
      </span>
      <span>
        {% if filters and filters.get("territory") %}
          حوزه فروش: {{ filters.get("territory") }}
        {% endif %}
      </span>
    </div>
  </div>

  <div class="blue-report-orange-line"></div>

  <table class="blue-report-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>
            {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr class="{% if row.get('is_group') %}blue-report-row-group{% endif %}">
        {% for col in columns %}
          {% set key = col.fieldname or col.label or ("col_" ~ loop.index0) %}
          {% set value = row.get(key) %}
          <td
            {% if loop.index0 == 0 and row.get("indent") %}
              style="padding-right: {{ (row.get('indent') or 0) * 16 }}px;"
            {% endif %}
          >
            {{ value }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="blue-report-footer-note">
    این چاپ بر اساس گزارش «گزارش تحلیلی فروش» در ERPNext تولید شده است. چیدمان و گروه‌بندی بر اساس تنظیمات خود گزارش است.
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOCTYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOCTYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_trial_balance_v1():
    """Create/Update BLUE_REPORT_TRIAL_BALANCE_V1 Print Format (Jinja) for Trial Balance report."""
    import frappe

    NAME = "BLUE_REPORT_TRIAL_BALANCE_V1"
    DOC_TYPE = "Report"

    HTML = """
<style>
  @font-face {
    font-family: "Vazirmatn";
    src: url("/files/Vazirmatn[wght].woff2") format("woff2");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  .btb-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .btb-title {
    text-align: center;
    font-size: 16pt;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .btb-subtitle {
    text-align: center;
    font-size: 9pt;
    margin-bottom: 8px;
  }

  .btb-meta {
    margin-bottom: 8px;
    font-size: 9pt;
  }

  .btb-meta div {
    margin-bottom: 2px;
  }

  .btb-meta-label {
    font-weight: 600;
    margin-left: 4px;
  }

  .btb-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
    font-size: 9pt;
  }

  .btb-table th,
  .btb-table td {
    border: 1px solid #dddddd;
    padding: 4px 5px;
  }

  .btb-table th {
    background-color: #fafafa;
    text-align: center;
    font-weight: 600;
  }

  .btb-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .btb-footer {
    margin-top: 12px;
    font-size: 8pt;
    text-align: left;
    direction: ltr;
  }
</style>

<div class="btb-root">

  <div class="btb-title">تراز آزمایشی</div>
  <div class="btb-subtitle">
    {% if report_name is defined and report_name %}
      {{ report_name }}
    {% else %}
      تراز آزمایشی (Trial Balance)
    {% endif %}
  </div>

  <div class="btb-meta">
    {% if filters is defined and filters %}
      {% if filters.get("company") %}
      <div>
        <span class="btb-meta-label">شرکت:</span>
        {{ filters.get("company") }}
      </div>
      {% endif %}

      {% set from_date = filters.get("from_date") %}
      {% set to_date = filters.get("to_date") %}
      {% if from_date or to_date %}
      <div>
        <span class="btb-meta-label">دوره گزارش:</span>
        {% if from_date %} از {{ from_date | as_jdate("YYYY/MM/DD") }} {% endif %}
        {% if to_date %} تا {{ to_date | as_jdate("YYYY/MM/DD") }} {% endif %}
      </div>
      {% endif %}
    {% endif %}
  </div>

  {% macro blue_tb_format_cell(row, col) -%}
    {% set val = row.get(col.fieldname) if col.fieldname else None %}
    {% if val is none %}
      {# empty #}
    {% elif col.fieldtype in ("Date", "Datetime") or col.fieldname in ("from_date", "to_date") %}
      {{ val | as_jdate("YYYY/MM/DD") }}
    {% elif col.fieldtype in ("Currency", "Float", "Int") %}
      {{ "{:,.0f}".format(val) }}
    {% else %}
      {{ val }}
    {% endif %}
  {%- endmacro %}

  <table class="btb-table">
    <thead>
      <tr>
        {% for col in columns %}
          <th>{{ col.label or col.fieldname }}</th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
          <td>
            {{ blue_tb_format_cell(row, col) }}
          </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="btb-footer">
    Generated by BlueAPI / blue_jdate — J2 (Jalali UI, Gregorian data)
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Print Format", NAME)
    if existing:
        pf = frappe.get_doc("Print Format", existing)
        pf.doc_type = DOC_TYPE
        pf.print_format_type = "Jinja"
        pf.custom_format = 1
        pf.disabled = 0
        pf.html = HTML
        pf.save()
    else:
        pf = frappe.get_doc({
            "doctype": "Print Format",
            "name": NAME,
            "doc_type": DOC_TYPE,
            "print_format_type": "Jinja",
            "custom_format": 1,
            "disabled": 0,
            "html": HTML,
        })
        pf.insert(ignore_permissions=True)

    frappe.db.commit()
    print("REPORT PRINT FORMAT:", pf.name, "created/updated.")

def create_blue_report_work_order_summary_v1():
    """Create/Update BLUE_REPORT_WORK_ORDER_SUMMARY_V1 based on standard 'Work Order Summary' report."""
    import frappe

    NAME = "BLUE_REPORT_WORK_ORDER_SUMMARY_V1"
    BASE_REPORT = "Work Order Summary"

    HTML = """
<style>
  .bapi-report-root {
    direction: rtl;
    text-align: right;
    font-family: "Vazirmatn", Tahoma, sans-serif;
    font-size: 10pt;
    color: #222;
    padding: 16px 20px;
  }

  .bapi-report-title {
    font-size: 18pt;
    font-weight: 700;
    margin-bottom: 6px;
    text-align: center;
  }

  .bapi-report-subtitle {
    font-size: 10pt;
    margin-bottom: 10px;
    text-align: center;
    color: #666;
  }

  .bapi-report-filters {
    font-size: 9pt;
    margin-bottom: 10px;
    border: 1px solid #eee;
    padding: 6px 8px;
    border-radius: 3px;
  }

  .bapi-report-filters-title {
    font-weight: 600;
    margin-bottom: 4px;
  }

  .bapi-report-filters-table {
    width: 100%;
    border-collapse: collapse;
  }

  .bapi-report-filters-table td {
    padding: 2px 4px;
    vertical-align: top;
  }

  .bapi-report-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 9.5pt;
    margin-top: 6px;
  }

  .bapi-report-table th,
  .bapi-report-table td {
    border: 1px solid #dddddd;
    padding: 4px 6px;
  }

  .bapi-report-table th {
    background-color: #fafafa;
    font-weight: 600;
    text-align: center;
    white-space: nowrap;
  }

  .bapi-report-table tbody tr:nth-child(odd) {
    background-color: #fcfcfc;
  }

  .bapi-report-footer {
    margin-top: 10px;
    font-size: 8.5pt;
    color: #777;
    text-align: left;
  }
</style>

<div class="bapi-report-root">

  <div class="bapi-report-title">
    گزارش خلاصه دستورهای تولید
  </div>
  <div class="bapi-report-subtitle">
    {{ report_name }}
  </div>

  {% if filters %}
  <div class="bapi-report-filters">
    <div class="bapi-report-filters-title">فیلترها:</div>
    <table class="bapi-report-filters-table">
      {% for key, value in filters.items() %}
      {% if value %}
      <tr>
        <td><strong>{{ key }}</strong></td>
        <td>{{ value }}</td>
      </tr>
      {% endif %}
      {% endfor %}
    </table>
  </div>
  {% endif %}

  <table class="bapi-report-table">
    <thead>
      <tr>
        {% for col in columns %}
        <th>
          {{ col.label or col.fieldname or ("ستون " ~ loop.index) }}
        </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody>
      {% for row in data %}
      <tr>
        {% for col in columns %}
        {% set fname = col.fieldname %}
        <td>
          {% if fname %}
            {{ row.get(fname) }}
          {% else %}
            {{ row.get(col.label) if row.get(col.label) is defined else "" }}
          {% endif %}
        </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="bapi-report-footer">
    چاپ شده توسط BlueAPI / BlueJDate
  </div>

</div>
    """.strip()

    existing = frappe.db.exists("Report", NAME)
    if existing:
        report = frappe.get_doc("Report", NAME)
    else:
        base = frappe.get_doc("Report", BASE_REPORT)
        report = frappe.copy_doc(base)

        # ستاپ پایه
        report.name = NAME
        report.report_name = NAME
        report.is_standard = "No"
        report.disabled = 0
        report.custom_format = 1

        # مهم: Letter Head دمو را خنثی کن که خطای LinkValidation ندهد
        if hasattr(report, "letter_head"):
            report.letter_head = None

        report.insert(ignore_permissions=True)

    # آپدیت نهایی
    report.is_standard = "No"
    report.disabled = 0
    report.custom_format = 1
    if hasattr(report, "letter_head"):
        report.letter_head = None
    report.html = HTML
    report.save()

    frappe.db.commit()
    print("REPORT:", report.name, "created/updated based on", BASE_REPORT)
